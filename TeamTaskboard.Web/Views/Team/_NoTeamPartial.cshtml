<section class="row">
    <p>Currently you are not part of a team.</p>
    <p>
        @Ajax.ActionLink("Join a team", "JoinTeam", null,
                new AjaxOptions
                {
                    UpdateTargetId = "team-update",
                    HttpMethod = "GET",
                    InsertionMode = InsertionMode.Replace
                }, new { @class = "btn btn-default" })
        &nbsp; or &nbsp;
        @Ajax.ActionLink("Create a team", "CreateTeam", null,
                new AjaxOptions
                {
                    UpdateTargetId = "team-update",
                    HttpMethod = "GET",
                    InsertionMode = InsertionMode.Replace
                }, new { @class = "btn btn-default" })
    </p>
</section>

<section class="row" id="team-update"></section>
